Keep Handshake / MISTI / CAPD exports here.

Accepted files (committed so Actions can ingest them twice a day):
  *.json  — array of listing objects, or { listings: [...] }
  *.csv   — header row; see README for column names

Do not commit cookies, Handshake session files, or anything scraped behind login.
